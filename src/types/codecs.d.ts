interface XOR {
    encode: string,
    decode: string
}

interface plain {
    encode: string,
    decode: string
}

interface base64 {
    encode: string,
    decode: string
}

export type { XOR, plain, base64 }